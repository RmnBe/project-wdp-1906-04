console.log('hello');

// Implementation of tiny-slider

// eslint-disable-next-line no-unused-vars,no-undef
const slider = tns({
  container: '.my-slider',
  slideBy: 'page',
  items: 2,
  autoplay: true
});
